using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JoystickMovement : MonoBehaviour
{
    //[SerializeField]
    //private Joystick moveJoystick;
    //[SerializeField]
    //private Joystick rotateJoystick;

    //[SerializeField]
    //private float moveSpeed = 5f;
    //[SerializeField]
    //private float moveForce = 5f;
    //private Rigibody rb;

    //void Start()
    //{
    //    rb = GetComponent<Rigidbody>();
    //}

    //private void Update()
    //{
    //    float x = moveJoystick.Horizontal;
    //    float z = moveJoystick.Vertical;
    //    float y = rotateJoystick.Vertical;

    //    Vector3 moveDirection = transform.right * x + transform.forward* z;
    //    rb.AddForce(moveDirection * Time.deltaTime, ForceMode.VeloctiyChange);
       
    //    Vector3 lookDirection = new Vector3(0, y, 0);
    //    transform.Rotate(lookDirection * moveSpeed * Time.deltaTime);
    //}
}
